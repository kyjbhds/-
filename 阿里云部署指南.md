# 学生成长管理系统 - 阿里云部署指南

## 📋 方案概述

采用 **阿里云静态网站托管 + 函数计算代理** 的方案，完全免费额度内满足个人使用需求。

- 前端：阿里云静态网站托管（免费）
- 后端API代理：阿里云函数计算 FC（免费额度）
- 数据存储：飞书多维表格（免费）

## 🎯 免费额度说明

阿里云个人免费套餐完全够用：
- 静态网站托管：无限（按流量计费，个人使用几乎免费）
- 函数计算：每月 100 万次调用 + 400,000 GB·秒 免费额度
- 流量：免费额度内完全够用

## 🚀 部署步骤

### 第一步：部署阿里云函数计算（API代理）

1. **登录阿里云控制台**
   - 访问 https://fcnext.console.aliyun.com
   - 选择 **"华东1（杭州）"** 或其他国内区域

2. **创建函数**
   - 点击 **"创建服务"**，服务名：`student-growth-service`
   - 点击 **"创建函数"** → **"使用内置运行时创建"**
   - 函数名称：`bitable-proxy`
   - 运行环境：`Node.js 18`
   - 触发器配置：**HTTP 触发器**
     - 触发器名称：`httpTrigger`
     - 认证方式：**匿名（无需认证）**
     - 请求方式：全选（GET, POST, PUT, DELETE, OPTIONS）

3. **配置函数代码**
   - 将 [aliyun-fc/index.js](file:///workspace/student-growth-system/aliyun-fc/index.js) 的内容复制到函数代码编辑器
   - 点击 **"保存并部署"**

4. **配置环境变量**
   - 进入函数 → **"配置"** → **"环境变量"**
   - 添加以下环境变量：
     ```
     FEISHU_APP_ID=cli_aa8472cc38a4dbd3
     FEISHU_APP_SECRET=yfY1qR3vaFqCxJB7ARTgfcOKxaGZQLts
     ```
   - 点击 **"保存"**

5. **获取函数访问地址**
   - 在函数详情页找到 **"访问地址"** 或 **"访问域名"**
   - 记录下来，例如：`https://bitable-proxy.cn-hangzhou.aliyuncs.com`
   - 访问路径是 `/api/bitable-proxy`，所以完整地址是：`https://xxx.cn-hangzhou.aliyuncs.com/api/bitable-proxy`

### 第二步：构建前端项目

1. **拉取最新代码**
   ```bash
   git clone https://github.com/kyjbhds/-.git student-growth-system
   cd student-growth-system/web
   ```

2. **配置环境变量**
   - 复制 `.env.example` 为 `.env`
   - 修改 `.env` 中的 `VITE_ALIYUN_FC_URL` 为你刚才获取的函数地址：
     ```env
     VITE_ALIYUN_FC_URL=https://bitable-proxy.cn-hangzhou.aliyuncs.com/api/bitable-proxy
     ```

3. **构建项目**
   ```bash
   npm install
   npm run build
   ```
   构建完成后，会在 `web/dist` 目录下生成静态文件

### 第三步：部署到阿里云静态网站托管

1. **开通静态网站托管**
   - 访问 https://oss.console.aliyun.com
   - 点击 **"创建 Bucket"**
   - Bucket名称：`student-growth-xxx`（xxx换成你的名字，全局唯一）
   - 地域：选择和函数计算相同的区域（如华东1杭州）
   - 存储类型：**标准存储**
   - 版本控制：**关闭**
   - 读写权限：**公共读**
   - 点击 **"确定创建"**

2. **配置静态网站托管**
   - 进入 Bucket → **"数据管理"** → **"静态页面"**
   - 点击 **"设置"**
   - 默认首页：`index.html`
   - 默认404页：`index.html`（单页应用需要）
   - 点击 **"确定"**

3. **上传前端文件**
   - 进入 Bucket → **"文件管理"** → **"上传文件"**
   - 点击 **"上传目录"**
   - 选择刚才构建的 `web/dist` 目录
   - 点击 **"上传"**

4. **获取访问地址**
   - 在静态页面设置页找到 **"访问地址"**
   - 复制保存，例如：`https://student-growth-xxx.oss-cn-hangzhou.aliyuncs.com`

### 第四步：配置自定义域名（可选但推荐）

1. **购买域名（如果需要）**
   - 访问 https://wanwang.aliyun.com
   - 搜索并购买一个便宜的域名（.com 约 50元/年，.cn 约 30元/年）

2. **配置域名解析**
   - 进入域名管理 → 你的域名 → **"解析设置"**
   - 添加记录：
     - 主机记录：`@` 或 `www`
     - 记录类型：`CNAME`
     - 记录值：你的 OSS 静态页面访问地址（去掉 https://）

3. **绑定域名到 OSS**
   - 进入 OSS Bucket → **"域名管理"**
   - 点击 **"绑定域名"**
   - 输入你的域名
   - 选择 **"自动添加 CNAME 记录"**
   - 点击 **"确定"**

## 🔧 配置检查清单

部署完成后，请确认以下内容：

- [ ] 函数计算已创建并部署成功
- [ ] 函数环境变量已正确配置
- [ ] 前端项目已构建，环境变量中配置了 `VITE_ALIYUN_FC_URL`
- [ ] 静态网站托管已配置，文件已上传
- [ ] 访问静态网站地址，页面能正常打开
- [ ] 尝试添加一条学生记录，确认 API 调用正常

## 💡 使用指南

### 本地开发
```bash
cd web
npm install
npm run dev
```
访问 `http://localhost:3000` 即可

### 生产环境更新
1. 本地修改代码
2. 重新构建：`npm run build`
3. 重新上传 dist 目录到 OSS
4. 刷新页面即可

## 🆘 常见问题

### Q: 为什么我的API请求失败了？
A: 检查以下几点：
1. 函数计算是否正常运行
2. 环境变量 `FEISHU_APP_ID` 和 `FEISHU_APP_SECRET` 是否正确
3. 飞书应用是否有 `bitable:record` 和 `bitable:app` 权限

### Q: 静态网站访问很慢？
A: 可以开启 CDN 加速，OSS 配置中有 CDN 选项，按流量计费很便宜。

### Q: 我可以用手机访问吗？
A: 当然可以！只要有网络，在手机浏览器输入静态网站地址即可访问。

## 📞 技术支持

如果部署遇到问题，可以：
1. 检查阿里云控制台的函数日志
2. 查看浏览器开发者工具的网络请求
3. 检查飞书应用权限配置

---

祝你部署顺利！🎉
