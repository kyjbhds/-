# 学生成长管理系统 - 阿里云部署指南

## 架构说明

```
用户浏览器
    ↓
前端 (OSS静态托管) - 提供HTML/CSS/JS
    ↓
后端API代理 (函数计算) - 解决CORS问题
    ↓
飞书API - 数据存储
```

## 准备工作

### 1. 获取必要信息

从项目 `/workspace/student-growth-system/web/.env` 文件中获取：
- 飞书 App ID: `cli_aa8472cc38a4dbd3`
- 飞书 App Secret: `yfY1qR3vaFqCxJB7ARTgfcOKxaGZQLts`
- Base Token: `XtkXbx2GTadruYsz0jFcsYjJn3c`

---

## 第一步：部署函数计算（API代理）

### 1.1 创建函数

1. 登录 [阿里云函数计算控制台](https://fcnext.console.aliyun.com/)
2. 选择地域：**华东1（杭州）**
3. 点击 **创建服务**
4. 服务名称：`student-growth-api`
5. 点击 **创建函数**

### 1.2 选择函数类型

选择 **事件函数**，点击下一步

### 1.3 配置函数

| 配置项 | 值 |
|--------|-----|
| 函数名称 | `bitable-proxy` |
| 运行环境 | `Node.js 18` |
| 代码上传方式 | ZIP包上传 |
| 启动命令 | `node index.js` |

### 1.4 函数代码

代码路径：`/workspace/student-growth-system/aliyun-fc/index.js`

```javascript
const http = require('http');
const https = require('https');
const url = require('url');

let cachedToken = null;
let tokenExpireTime = 0;

function getTenantAccessToken(appId, appSecret) {
    return new Promise((resolve, reject) => {
        if (cachedToken && Date.now() < tokenExpireTime - 5 * 60 * 1000) {
            return resolve(cachedToken);
        }

        const data = JSON.stringify({
            app_id: appId,
            app_secret: appSecret
        });

        const options = {
            hostname: 'open.feishu.cn',
            port: 443,
            path: '/open-apis/auth/v3/tenant_access_token/internal',
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Content-Length': data.length
            }
        };

        const req = https.request(options, (res) => {
            let body = '';
            res.on('data', (chunk) => { body += chunk; });
            res.on('end', () => {
                try {
                    const result = JSON.parse(body);
                    if (result.code === 0) {
                        cachedToken = result.tenant_access_token;
                        tokenExpireTime = Date.now() + result.expire * 1000;
                        resolve(cachedToken);
                    } else {
                        reject(new Error(`获取token失败: ${result.msg}`));
                    }
                } catch (e) {
                    reject(e);
                }
            });
        });

        req.on('error', reject);
        req.write(data);
        req.end();
    });
}

function proxyToFeishu(path, method, token, body) {
    return new Promise((resolve, reject) => {
        const targetPath = `/open-apis/bitable/v1${path}`;
        const requestData = body ? JSON.stringify(body) : '';

        const options = {
            hostname: 'open.feishu.cn',
            port: 443,
            path: targetPath,
            method: method,
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${token}`,
                ...(body ? { 'Content-Length': requestData.length } : {})
            }
        };

        const req = https.request(options, (res) => {
            let responseData = '';
            res.on('data', (chunk) => { responseData += chunk; });
            res.on('end', () => {
                resolve({
                    statusCode: res.statusCode,
                    data: responseData
                });
            });
        });

        req.on('error', reject);
        if (body) {
            req.write(requestData);
        }
        req.end();
    });
}

const server = http.createServer(async (req, res) => {
    res.setHeader('Access-Control-Allow-Origin', '*');
    res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS');
    res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');

    if (req.method === 'OPTIONS') {
        res.writeHead(200);
        res.end();
        return;
    }

    const parsedUrl = url.parse(req.url, true);
    let targetPath = parsedUrl.pathname;

    if (targetPath.startsWith('/api/bitable-proxy')) {
        targetPath = targetPath.replace('/api/bitable-proxy', '');
    }
    if (!targetPath) {
        targetPath = '/';
    }

    if (targetPath === '/' || targetPath === '/health') {
        res.writeHead(200, { 'Content-Type': 'application/json' });
        res.end(JSON.stringify({ status: 'ok', message: 'Proxy is running' }));
        return;
    }

    try {
        const appId = process.env.FEISHU_APP_ID;
        const appSecret = process.env.FEISHU_APP_SECRET;

        if (!appId || !appSecret) {
            res.writeHead(500, { 'Content-Type': 'application/json' });
            res.end(JSON.stringify({ error: 'Missing FEISHU credentials' }));
            return;
        }

        const token = await getTenantAccessToken(appId, appSecret);

        let requestBody = null;
        if (req.method !== 'GET' && req.method !== 'HEAD') {
            const chunks = [];
            for await (const chunk of req) {
                chunks.push(chunk);
            }
            const bodyStr = Buffer.concat(chunks).toString();
            if (bodyStr) {
                try {
                    requestBody = JSON.parse(bodyStr);
                } catch (e) {
                    requestBody = bodyStr;
                }
            }
        }

        const result = await proxyToFeishu(targetPath, req.method, token, requestBody);

        let responseBody;
        try {
            const parsed = JSON.parse(result.data);
            responseBody = JSON.stringify(parsed);
        } catch (e) {
            responseBody = result.data;
        }

        res.writeHead(result.statusCode, { 'Content-Type': 'application/json' });
        res.end(responseBody);
    } catch (error) {
        console.error('Proxy error:', error);
        res.writeHead(500, { 'Content-Type': 'application/json' });
        res.end(JSON.stringify({
            error: 'Proxy request failed',
            message: error.message
        }));
    }
});

const PORT = process.env.PORT || 9000;
server.listen(PORT, () => {
    console.log(`Server running on port ${PORT}`);
});
```

### 1.5 package.json

```json
{
  "name": "bitable-proxy",
  "version": "1.0.0",
  "description": "飞书Base API代理",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {}
}
```

### 1.6 配置环境变量

在函数配置页面，找到 **环境变量**，添加：

| 变量名 | 值 |
|--------|-----|
| `FEISHU_APP_ID` | `cli_aa8472cc38a4dbd3` |
| `FEISHU_APP_SECRET` | `yfY1qR3vaFqCxJB7ARTgfcOKxaGZQLts` |

### 1.7 创建HTTP触发器

1. 在函数详情页，点击 **触发器** 标签
2. 点击 **创建触发器**
3. 配置：
   - 触发器类型：**HTTP触发器**
   - 认证方式：**anonymous**（无需认证）
   - 请求方式：**GET, POST, PUT, DELETE, OPTIONS**
4. 点击 **确定**

### 1.8 获取函数URL

创建成功后，在触发器页面会显示函数URL，记录下来。

---

## 第二步：部署静态网站到OSS

### 2.1 创建OSS Bucket

1. 登录 [阿里云OSS控制台](https://oss.console.aliyun.com/)
2. 点击 **创建Bucket**
3. 配置：
   - Bucket名称：`student-growth-xxx`（xxx是你的标识）
   - 地域：选择 **华东1（杭州）**
   - 存储类型：**标准存储**
   - 访问权限：**公共读**

### 2.2 上传前端文件

构建后的文件在：`/workspace/student-growth-system/web/dist`

1. 进入Bucket，点击 **上传文件**
2. 将 `dist` 目录下的所有文件拖拽上传

### 2.3 配置静态网站托管

1. 点击 **基础设置** → **静态网站托管**
2. 点击 **设置**
3. 配置：
   - 默认首页：**index.html**
   - 默认 404 页：**index.html**
4. 点击 **保存**

### 2.4 获取访问地址

在 **静态网站托管** 页面，查看 **默认域名**

---

## 第三步：更新前端配置

在 `.env.production` 文件中填入函数URL：

```
VITE_ALIYUN_FC_URL=https://你的函数URL/api/bitable-proxy
```

然后重新构建并上传。

---

## 第四步：配置CORS

在OSS Bucket中：

1. 点击 **数据安全** → **跨域设置**
2. 点击 **创建规则**
3. 配置：
   - 来源：`*`
   - 允许Methods：**GET, POST, PUT, DELETE, OPTIONS**
   - 允许Headers：`*`
4. 点击 **确定**

---

## 第五步：飞书开放平台配置

### 5.1 配置应用权限

确保已开启：
- `bitable:app` - 读取多维表格

### 5.2 配置CORS白名单

1. 在应用页面，点击 **安全设置**
2. 添加你的OSS访问地址到白名单

---

## 成本估算

| 服务 | 费用 |
|------|------|
| OSS存储 | 免费（5GB） |
| OSS流量 | ~¥0.5/GB |
| 函数计算 | 免费（100万次/月） |
| **总计** | **≈ ¥0** |

---

## 部署完成

访问你的OSS静态网站地址即可使用！
