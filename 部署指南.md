# 学生成长管理系统 - 部署指南

## 当前架构

- **前端**: React + TypeScript + Vite，部署在阿里云OSS
- **后端**: 阿里云函数计算（HTTP触发器），作为飞书Base API代理
- **数据库**: 飞书多维表格（Base）
- **自动部署**: GitHub Actions

## 访问地址

- 前端网站: https://myvip.oss-cn-hangzhou.aliyuncs.com
- 后端API: https://studentowth-api-jlpwzjexhc.cn-hangzhou.fcapp.run

## 部署流程

### 1. GitHub Secrets 配置

在 GitHub 仓库的 `Settings` → `Secrets and variables` → `Actions` 中添加以下 Secrets：

#### 必需的环境变量

1. **ALIYUN_ACCESS_KEY_ID**
   - 阿里云访问密钥ID
   - 获取位置：阿里云控制台 → AccessKey管理

2. **ALIYUN_ACCESS_KEY_SECRET**
   - 阿里云访问密钥Secret
   - 获取位置：阿里云控制台 → AccessKey管理

3. **VITE_BASE_TOKEN**
   - 飞书Base应用Token
   - 从飞书多维表格URL中获取
   - 示例：`XtkXbx2GTadruYsz0jFcsYjJn3c`

4. **VITE_FEISHU_APP_ID**
   - 飞书应用App ID
   - 获取位置：飞书开放平台 → 应用详情
   - 值：`cli_aa8472cc38a4dbd3`

5. **VITE_FEISHU_APP_SECRET**
   - 飞书应用App Secret
   - 获取位置：飞书开放平台 → 应用详情
   - 值：`yfY1qR3vaFqCxJB7ARTgfcOKxaGZQLts`

6. **VITE_FEISHU_CHAT_ID**
   - 飞书群聊ID（用于发送通知）
   - 值：`oc_e397720d1c21e9059ab16fe8b1c60c21`

7. **VITE_GLM_API_KEY**
   - 智谱AI GLM-4-Flash API密钥
   - 用于AI智能识别功能
   - 值：`5745e63ffa244ceea335b0320a8e24b5.pNd5KbfeSm902lrg`

8. **VITE_ADMIN_PASSWORD**
   - 管理后台登录密码
   - 请设置一个安全的密码

9. **VITE_ALIYUN_FC_URL**
   - 阿里云函数计算API地址
   - 值：`https://studentowth-api-jlpwzjexhc.cn-hangzhou.fcapp.run/api/bitable-proxy`

### 2. GitHub Actions 自动部署

代码推送到 `main` 分支后，GitHub Actions 会自动：

1. 构建前端项目
2. 部署前端到阿里云OSS（设置正确的Content-Type）
3. 打包并部署函数计算代码
4. 更新函数计算环境变量

查看部署状态：https://github.com/kyjbhds/-/actions

### 3. 手动触发部署

如果需要手动触发部署：
1. 进入 GitHub 仓库
2. 点击 `Actions` 标签
3. 选择 `Deploy to Aliyun (Full Stack)` 工作流
4. 点击 `Run workflow`

## 手动部署步骤

### 部署前端到OSS

```bash
# 安装ossutil
wget https://gosspublic.alicdn.com/ossutil/1.7.19/ossutil64
chmod 755 ossutil64
sudo mv ossutil64 /usr/local/bin/ossutil

# 配置ossutil
ossutil config -e oss-cn-hangzhou.aliyuncs.com -i <你的AccessKeyId> -k <你的AccessKeySecret>

# 构建项目
cd web
npm install
npm run build

# 部署HTML文件
cd dist
for file in $(find . -name "*.html"); do
  ossutil cp "$file" oss://myvip/${file#./} --meta "Content-Type:text/html; charset=utf-8" --force
done

# 部署其他文件...
```

### 部署函数计算

```bash
# 安装阿里云CLI
curl -O https://aliyuncli.alicdn.com/aliyun-cli-linux-latest-amd64.tgz
tar -xvf aliyun-cli-linux-latest-amd64.tgz
sudo mv aliyun /usr/local/bin/

# 配置阿里云CLI
aliyun configure set --access-key-id <你的AccessKeyId> --access-key-secret <你的AccessKeySecret> --region cn-hangzhou

# 打包函数代码
cd aliyun-fc
zip -r function-code.zip index.js package.json

# 更新函数
aliyun fc20220630 UpdateFunction \
  --region cn-hangzhou \
  --service-name my-vip-service \
  --function-name my-vip \
  --code zipFile@function-code.zip
```

## 功能说明

### 已实现功能

1. ✅ **学生档案管理** - 添加、查看学生信息（包括地址字段）
2. ✅ **课程记录** - 添加、查看课程记录
3. ✅ **知识点管理** - 添加、查看知识点
4. ✅ **成长记录可视化** - 雷达图、成长曲线
5. ✅ **AI智能识别** - 通过图片识别课堂内容（需要配置GLM API）
6. ✅ **数据导出** - 导出PDF报告
7. ✅ **飞书机器人助手** - 发送通知到群聊

### 注意事项

1. **OSS静态网站配置**
   - 需要在OSS控制台开启静态网站托管
   - 设置默认首页为 `index.html`
   - 设置404页面为 `index.html`

2. **函数计算配置**
   - 确保函数计算有HTTP触发器
   - 配置环境变量：`FEISHU_APP_ID` 和 `FEISHU_APP_SECRET`
   - 设置正确的内存和超时时间

3. **CORS配置**
   - OSS需要配置CORS规则，允许前端域名访问
   - 函数计算HTTP触发器已配置CORS头部

4. **安全性**
   - 所有敏感信息存储在GitHub Secrets中
   - 生产环境不要在前端暴露敏感信息
   - 定期轮换Access Key

## 常见问题

### 1. 添加学生无响应
- 检查浏览器控制台错误信息
- 确认后端API是否正常运行
- 检查网络连接

### 2. OSS访问下载HTML文件
- 确认OSS Bucket的静态网站配置
- 检查文件的Content-Type是否正确
- 查看OSS的CORS配置

### 3. API返回健康检查信息
- 函数代码可能未正确部署
- 检查环境变量配置
- 查看函数计算日志

### 4. 部署失败
- 检查GitHub Secrets配置
- 确认阿里云凭证有效
- 查看Actions日志详情

## 联系方式

如有问题，请检查：
- GitHub Actions 运行日志
- 阿里云函数计算日志
- 浏览器开发者工具控制台
