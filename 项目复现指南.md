# 学生成长管理系统 - 项目复现指南

## 一、项目概述

**项目名称**：学生成长管理系统（Student Growth Management System）

**目标用户**：独立教师（家教/培训机构老师）

**核心功能**：
- 管理学生学习档案和成长记录
- 记录每次上课内容、表现、作业完成情况
- AI智能识别课堂照片，自动生成课程记录
- 生成学生成长报告（雷达图、成长曲线）
- 向家长发送学习反馈（飞书机器人）
- PDF报告导出

---

## 二、技术栈

| 层级 | 技术 |
|------|------|
| 前端框架 | React 18 + TypeScript + Vite |
| UI样式 | Tailwind CSS + Lucide React图标 |
| 数据可视化 | Recharts（雷达图、折线图） |
| PDF导出 | jsPDF + html2canvas |
| AI服务 | 智谱GLM-4-Flash（免费，中文好） |
| 数据存储 | 飞书多维表格（Base） |
| 消息推送 | 飞书机器人（Webhook） |

---

## 三、飞书Base数据结构

创建1个Base，包含6张表：

### 表1：学生表（tblStudents）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 学生姓名 | 文本 | |
| 年级 | 单选 | 初一/初二/初三/高一/高二/高三 |
| 科目 | 多选 | 数学/物理/化学/生物 |
| 入学时间 | 日期 | |
| 家长联系方式 | 文本 | 手机号或微信 |
| 当前状态 | 单选 | 在读/暂停/结课 |
| 总课时数 | 数字 | 公式计算 |
| 最近上课日期 | 日期 | |

### 表2：课程记录表（tblLessons）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 关联学生 | 关联字段→学生表 | |
| 上课日期 | 日期 | |
| 科目 | 单选 | |
| 课程主题 | 文本 | |
| 课堂表现评分 | 数字 | 1-5分 |
| 作业完成情况 | 单选 | 优秀/良好/一般/未完成 |
| 课堂照片 | 附件 | |
| 教师备注 | 多行文本 | |
| 下节课计划 | 多行文本 | |

### 表3：知识点表（tblKnowledge）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 知识点名称 | 文本 | |
| 所属科目 | 单选 | |
| 所属年级 | 单选 | |
| 难度等级 | 单选 | 基础/中等/提高/竞赛 |

### 表4：学生知识点掌握表（tblStudentKnowledge）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 关联学生 | 关联字段→学生表 | |
| 关联知识点 | 关联字段→知识点表 | |
| 掌握程度 | 单选 | 未掌握/了解/掌握/熟练 |
| 首次接触日期 | 日期 | |
| 最近巩固日期 | 日期 | |

### 表5：资料表（tblMaterials）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 资料名称 | 文本 | |
| 资料类型 | 单选 | 讲义/习题/试卷/笔记 |
| 关联科目 | 多选 | |
| 关联年级 | 多选 | |
| 文件 | 附件 | |
| 适用场景 | 多选 | 新课/复习/考前/竞赛 |

### 表6：阶段记录表（tblMilestones）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 关联学生 | 关联字段→学生表 | |
| 阶段类型 | 单选 | 月度/学期/年度 |
| 开始日期 | 日期 | |
| 结束日期 | 日期 | |
| 阶段评价 | 多行文本 | |
| 报告状态 | 单选 | 未生成/已生成/已发送 |

---

## 四、前端页面结构

```
/src
  /components
    Layout.tsx      # 侧边栏布局
    Login.tsx       # 密码登录
    Sidebar.tsx     # 导航菜单
  /pages
    Dashboard.tsx       # 仪表盘（统计+快捷录入）
    StudentList.tsx     # 学生列表
    StudentDetail.tsx   # 学生详情（基本信息+课时统计）
    StudentReport.tsx   # 成长报告（雷达图+曲线+AI评语）
    StudentTimeline.tsx # 学习轨迹时间线
    LessonList.tsx      # 课程记录列表
    LessonRecord.tsx    # 手动录入课程
    AiLessonRecord.tsx  # AI智能识别录入
    KnowledgeList.tsx   # 知识点库
    MaterialList.tsx    # 资料库
    PrepareLesson.tsx   # 课前准备（薄弱点+推荐资料）
    DataBackup.tsx      # 数据备份导出
    FeishuBotAssistant.tsx # 飞书机器人配置测试
  /utils
    baseApi.ts        # 飞书Base API封装
    aiRecognition.ts  # AI图片识别
    aiComment.ts      # AI评语生成
    pdfExport.ts      # PDF导出
    feishuBot.ts      # 飞书机器人消息推送
  /types
    index.ts          # TypeScript类型定义
  App.tsx             # 路由配置
```

---

## 五、核心功能实现要点

### 1. 飞书API封装（baseApi.ts）

```typescript
// 环境变量
const BASE_TOKEN = import.meta.env.VITE_BASE_TOKEN; // 飞书Base的token
const APP_ID = import.meta.env.VITE_FEISHU_APP_ID;
const APP_SECRET = import.meta.env.VITE_FEISHU_APP_SECRET;

// 获取tenant_access_token
async function getToken() {
  const res = await fetch('https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ app_id: APP_ID, app_secret: APP_SECRET })
  });
  const data = await res.json();
  return data.tenant_access_token;
}

// 请求封装
async function baseRequest(endpoint: string, options: RequestInit = {}) {
  const token = await getToken();
  const url = `https://open.feishu.cn/open-apis/bitable/v1/apps/${BASE_TOKEN}${endpoint}`;
  const res = await fetch(url, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`,
      ...options.headers,
    },
  });
  return res.json();
}

// CRUD操作
export const listRecords = (tableId: string, params?: Record<string, string>) => 
  baseRequest(`/tables/${tableId}/records?${new URLSearchParams(params)}`);

export const createRecord = (tableId: string, fields: any) => 
  baseRequest(`/tables/${tableId}/records`, { method: 'POST', body: JSON.stringify({ fields }) });

export const updateRecord = (tableId: string, recordId: string, fields: any) => 
  baseRequest(`/tables/${tableId}/records/${recordId}`, { method: 'PUT', body: JSON.stringify({ fields }) });

export const deleteRecord = (tableId: string, recordId: string) => 
  baseRequest(`/tables/${tableId}/records/${recordId}`, { method: 'DELETE' });
```

### 2. AI图片识别（aiRecognition.ts）

使用GLM-4-Flash API，传入课堂照片base64，返回：
- 课程主题
- 科目
- 知识点列表
- 课堂情况描述
- 学生表现评分（1-5分）

### 3. AI评语生成（aiComment.ts）

根据学生历史表现数据，调用GLM-4-Flash生成150-200字个性化评语。

### 4. 成长报告（StudentReport.tsx）

- **雷达图**：展示各维度能力（课堂表现、作业完成、知识掌握、学习态度、进步速度）
- **成长曲线**：折线图展示历次课程评分变化
- **AI评语**：自动生成教师评语
- **PDF导出**：一键导出完整报告

### 5. 飞书机器人推送（feishuBot.ts）

发送交互式卡片消息到家长群，包含：
- 课程反馈（日期、科目、主题、评分、作业、评语）
- 阶段报告（总课时、平均分、知识点掌握、综合评价）
- 课前提醒（薄弱点、推荐资料、下次上课时间）

---

## 六、环境变量配置（.env）

```
VITE_BASE_TOKEN=你的飞书Base_Token
VITE_FEISHU_APP_ID=cli_xxxxxxxx
VITE_FEISHU_APP_SECRET=xxxxxxxx
VITE_FEISHU_CHAT_ID=oc_xxxxxxxx
VITE_GLM_API_KEY=xxxxxxxx
VITE_ADMIN_PASSWORD=你的登录密码
```

---

## 七、部署方式

### 方式1：本地使用
直接运行 `npm run dev`，本地访问

### 方式2：静态托管（推荐）
构建后部署到：
- GitHub Pages（免费）
- Netlify（免费）
- Vercel（免费，但国内可能访问慢）
- 阿里云OSS（国内访问快，有费用）

### 方式3：PWA（手机使用）
- 支持离线缓存
- 可添加到手机桌面
- 像原生App一样使用

---

## 八、关键依赖

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "lucide-react": "^0.294.0",
    "recharts": "^2.10.0",
    "jspdf": "^2.5.1",
    "html2canvas": "^1.4.1",
    "tailwindcss": "^3.3.0"
  }
}
```

---

## 九、复现步骤

1. **创建飞书应用**
   - 访问飞书开放平台
   - 创建企业自建应用
   - 开启"多维表格"权限
   - 获取App ID和App Secret

2. **创建Base和表格**
   - 按上面的结构创建6张表
   - 开启分享，获取Base Token

3. **初始化项目**
   ```bash
   npm create vite@latest student-growth -- --template react-ts
   cd student-growth
   npm install
   npm install react-router-dom lucide-react recharts jspdf html2canvas
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
   ```

4. **按上面的文件结构开发各个页面**

5. **配置环境变量**

6. **构建部署**
   ```bash
   npm run build
   # 将dist目录部署到静态托管
   ```

---

## 十、注意事项

1. **CORS问题**：飞书API支持CORS，但token需要妥善保管
2. **图片上传**：飞书Base附件上传需要额外的上传接口
3. **数据安全**：App Secret不要暴露在前端，生产环境需要后端代理
4. **AI费用**：GLM-4-Flash目前免费，但有速率限制

---

## 十一、手机App方案

### 方案A：PWA（渐进式Web应用）
- 无需开发原生App
- 支持离线使用
- 可添加到手机桌面
- 一套代码，多端使用

### 方案B：React Native
- 真正的原生App体验
- 需要额外学习成本
- 可以调用手机原生功能（相机、通知等）

### 方案C：Flutter
- 跨平台（iOS+Android）
- 性能接近原生
- 需要学习Dart语言

**推荐**：如果追求快速上线和低成本，选择PWA；如果需要原生体验和复杂功能，选择Flutter。
