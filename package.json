{
  "name": "student-growth-system-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "dev": "cd web && npm run dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
